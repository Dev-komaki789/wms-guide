import type { ReactChapter } from './types'

// 第15章「再レンダリングと最適化 ― memo と『なぜ無駄に描き直すのか』」。プログラミング初心者向け。
export const ch15Rerender: ReactChapter = {
  id: 'rerender',
  num: 15,
  title: '再レンダリングと最適化 ― memo と「なぜ無駄に描き直すのか」',
  summary: '「画面が変わる＝部品をもう一度実行する」という再レンダリングの正体を、ここで腰を据えて理解します。そして、必要のない描き直しを減らす React.memo / useCallback を、使いどころとともに学びます。最終章です。',
  intro: [
    'これまで何度も「再レンダリング（描き直し）」という言葉が出てきました。最後にこれを正面から理解しましょう。仕組みが分かると、「なぜか遅い」「無駄に動く」の原因をたどれるようになります。',
    'そのうえで、描き直しを減らす道具（React.memo / useCallback）を紹介します。ただし大事な前提：ほとんどの場合、最適化は要りません。「困ってから」使う道具です。',
  ],
  sections: [
    {
      id: 'what',
      heading: '15-1. 再レンダリングとは「部品の関数をもう一度呼ぶこと」',
      body: [
        '再レンダリングとは、その部品の関数がもう一度実行され、新しい JSX が作られることです。実際に画面（DOM）を書きかえるのは、前回との差分があった所だけ。だから速いのですが、「関数の再実行」自体は起きています。',
        '再レンダリングが起きるきっかけは主に2つ。①自分の state が変わったとき。②自分の親が再レンダリングされたとき（＝子もつられて描き直される）。',
      ],
      mermaid: `flowchart TD\n  A["親が再レンダリング"] --> B["子も再レンダリング"]\n  B --> C["孫も再レンダリング"]\n  D["state が変わる"] --> A`,
      callouts: [
        {
          kind: 'note',
          text: 'ポイント：親が描き直されると、props が変わっていなくても子は基本つられて描き直されます。たいていは問題になりませんが、子が重い場合に効いてきます。',
        },
      ],
    },
    {
      id: 'not-bad',
      heading: '15-2. 再レンダリングは「悪」ではない',
      body: [
        'まず誤解を解いておきます。再レンダリング自体は React の正常な動きで、ほとんどの場合とても速く、気にする必要はありません。',
        '問題になるのは「とても重い部品が」「とても頻繁に」「無駄に」描き直されるときだけ。最適化は、実際に遅さを感じて、計測して原因を特定してから行うものです。早すぎる最適化は、コードを読みにくくするだけで効果はありません。',
      ],
      callouts: [
        {
          kind: 'warn',
          text: 'やりがちな失敗：体感で遅くないのに、あちこちに memo や useCallback を付けてしまう。読みにくくなるうえ、ほぼ無意味です。まずは素直に書く。これが正解です。',
        },
      ],
    },
    {
      id: 'memo',
      heading: '15-3. React.memo ― props が同じなら描き直さない',
      body: [
        'React.memo で部品を包むと、「props が前回と変わっていなければ、親が再描画されても自分は描き直さない」ようになります。重い子部品が、関係ない親の更新で何度も描き直されるのを防げます。',
      ],
      examples: [
        {
          code: `import { memo } from 'react'\n\n// props（product）が変わらなければ再描画をスキップする\nconst ProductCard = memo(function ProductCard({ product }) {\n  return (\n    <div className="card">\n      <h2>{product.name}</h2>\n      <p>{product.price} 円</p>\n    </div>\n  )\n})`,
          note: 'memo で包むと、product が同じ間は描き直されません。ただし「比較するコスト」もかかるので、軽い部品にむやみに付けても得はしません。重くて・頻繁に描き直される部品に絞って使います。',
        },
      ],
    },
    {
      id: 'usecallback',
      heading: '15-4. useCallback ― 関数を「同じもの」に保つ',
      body: [
        'React.memo には落とし穴があります。親が関数を props で子に渡していると、再レンダリングのたびに“新しい関数”が作られ、「props が変わった」と見なされて memo が効きません。',
        'useCallback(() => {...}, [依存]) は、依存配列が変わらないかぎり「同じ関数」を使い回します。これで memo した子に渡しても、関数が毎回別物にならず、スキップが効くようになります。',
      ],
      examples: [
        {
          code: `import { useCallback } from 'react'\n\nfunction ProductList({ products }) {\n  // この関数は毎回作り直されず、同じものが使い回される\n  const handleAdd = useCallback((id) => {\n    console.log('カートに追加:', id)\n  }, [])\n\n  return products.map((p) => (\n    <ProductCard key={p.id} product={p} onAdd={handleAdd} />\n  ))\n}`,
          note: 'useCallback が無いと handleAdd は毎回“新しい関数”になり、memo した ProductCard が結局毎回描き直されます。memo と useCallback はセットで効く、と覚えましょう。',
        },
      ],
      callouts: [
        {
          kind: 'tip',
          text: 'useMemo（値を覚える）・useCallback（関数を覚える）・memo（部品の再描画を抑える）は3兄弟。どれも「困ってから・計測してから」。まずは素直に書き、遅さを感じたら原因を特定して、ピンポイントで使うのが正しい順番です。',
        },
      ],
    },
    {
      id: 'practical',
      heading: '15-5. 最適化の前にできること',
      body: [
        'memo などに手を出す前に、効果が大きくて副作用の少ない対策があります。これらの方が先です。',
      ],
      table: {
        headers: ['対策', '効果'],
        rows: [
          ['state を「使う場所の近く」に置く', '状態変更の影響範囲が狭まり、描き直しが減る'],
          ['重いリストはページング（第8章）', '一度に描く件数を減らす'],
          ['本当に重い計算だけ useMemo', 'ムダな再計算を避ける'],
          ['まず開発者ツールで計測する', '「どこが遅いか」を当て推量で決めない'],
        ],
      },
    },
    {
      id: 'closing',
      heading: '15-6. ここまでのまとめ（基礎編の区切り）',
      body: [
        'お疲れさまでした。第1〜6章で「部品・props・リスト・条件・状態・イベント」という土台を、第7〜12章で「副作用・ルーティング・Context・カスタムフック・通信・型」という実アプリの骨組みを、第13〜15章で「一歩進んだフックと最適化」を学びました。ここまでが基礎編の区切りです。',
        'React の核心は、ずっと一貫しています。「画面は状態の写し鏡。状態を変えれば画面がついてくる」。困ったら、この出発点に戻ってください。',
        '次の第16章からは応用編です。スタイリング（見た目の付け方）、堅牢化（エラーバウンダリ・遅延読み込み）、state 設計、そしてまわりの道具（エコシステム）へと進みます。',
      ],
    },
  ],
}
